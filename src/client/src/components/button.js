import React from 'react';

const button = ({textBtn}) => {
    return (
        <div class="bg-primary  text-center w-1/4 mx-auto py-2 rounded-md font-bold text-lg text-white">{textBtn}</div>
    );
};

export default button;